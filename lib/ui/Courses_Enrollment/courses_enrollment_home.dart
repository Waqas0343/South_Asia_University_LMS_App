import 'package:flutter/material.dart';

class CoursesEnrollmentHome extends StatelessWidget {
  const CoursesEnrollmentHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Enrollment Courses"),
      ),
    );
  }
}
