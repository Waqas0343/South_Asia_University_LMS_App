import 'package:flutter/material.dart';

class ExamScheduleHome extends StatelessWidget {
  const ExamScheduleHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Exam Schedule"),
      ),
    );
  }
}
