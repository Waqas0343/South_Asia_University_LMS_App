import 'package:flutter/material.dart';

class StudentHostelHome extends StatelessWidget {
  const StudentHostelHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Student Hostel"),
      ),
    );
  }
}
