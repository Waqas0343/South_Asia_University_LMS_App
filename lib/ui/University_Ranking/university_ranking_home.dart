import 'package:flutter/cupertino.dart';

class UniversityRankingHome extends StatelessWidget {
  const UniversityRankingHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
